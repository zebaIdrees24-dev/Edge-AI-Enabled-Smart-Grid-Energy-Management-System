# Notebooks

- [`01_research_benchmarks.ipynb`](01_research_benchmarks.ipynb) — executable research walkthrough covering causal feature engineering, forecasting baselines, rolling-origin validation, feature ablations, anomaly-detector comparison, feature importance, and accuracy/efficiency trade-offs.

The notebook uses the repository's synthetic telemetry generator so it can run without downloading external data. For the optional PyTorch LSTM benchmark, run the CLI research suite with the `deep-learning` extra installed.
